# 실습 안내

심평원 규정이 바뀌었을 때 **그 환자의 청구가 그대로 통과하는지**를, GitHub이 단계마다
확인해 주는 과정으로 직접 돌려 봅니다. 코드를 쓰지 않습니다. 버튼을 누르고, 읽고,
승인하면 됩니다.

---

## 이 실습에서 만드는 것

AI에게 "이 환자 청구 봐 줘"라고 한 번 물어보는 것과, 아래처럼 **단계마다 근거를 남기는
것**의 차이를 직접 보게 됩니다.

| 단계 | 무슨 일이 일어나나 | 내가 하는 일 |
|---|---|---|
| 1. 규정 수집 | 심평원 공고에서 청구에 닿는 변경만 골라 PR로 올린다 | 실행 · 읽고 머지 |
| 2. 요청 | 환자 번호 하나를 넣는다 | 입력 |
| 3. 조회 | 그 번호로 조회하고, 결과에서 번호를 뺀 뒤 AI에게 넘긴다 | (자동) |
| 4. 판단 | 진료일과 시행일을 대조해 청구 가능 여부를 가른다 | 보고서 읽기 |
| 5. 확인 | 저장소에 무엇이 남았는지 본다 | 확인 |

4단계가 핵심입니다. **같은 코드라도 진료일에 따라 답이 달라집니다.** 규정은 공고된
다음 날부터 자동으로 적용되지 않기 때문입니다.

---

## 미리 알아두실 것

- **AI가 한 번 판단하는 데 3~6분 걸립니다.** 화면이 멈춘 것처럼 보여도 정상입니다.
  기다리는 동안 다음 설명을 들으시면 됩니다.
- **모델은 무료 요금제면 `auto`, 유료 Copilot 이면 `haiku` 를 고르세요.**
- **환자 데이터는 가짜입니다.** 환자 50명 · 진료 50건의 합성 데이터입니다. 수가코드와
  규정만 실제입니다.
- **모든 번호에 진료내역이 있지는 않습니다.** 아래에 적힌 번호로 시작하세요.
- **AI는 데이터베이스에 접속하지 않습니다.** 조회는 워크플로가 끝내고, 그 결과에서 환자
  번호를 뺀 뒤에 넘깁니다.

---

## 준비 (약 4분)

### 1. 내 저장소 만들기

**https://github.com/t-hajongkim/hira-billing-copilot** 로 들어가서 초록색
**Use this template** → **Create a new repository** 를 누릅니다.

- Repository name: 아무 이름
- **Create repository** 클릭

### 2. 데이터베이스 비밀번호 등록

**Settings** → 왼쪽 **Secrets and variables** → **Actions** → **New repository secret**

- Name: `LLM_DB_PASSWORD`
- Secret: 아무 문자열 (예: `hands-on-2026`)
- **Add secret** 클릭

> 환자 데이터는 원본 저장소의 공개 이미지에서 옵니다. 그 이미지를 읽는 역할의 비밀번호만
> 각자 정합니다. 빠뜨리면 데이터베이스가 준비되지 않습니다.

### 3. AI가 풀 리퀘스트를 만들 수 있게 허용

**Settings** → 왼쪽 **Actions** → **General** → 맨 아래 *Workflow permissions* 에서

- **Allow GitHub Actions to create and approve pull requests** 체크
- **Save** 클릭

> 이걸 빠뜨리면 실습 1의 규정 수집이 일은 다 해놓고 PR을 올리지 못합니다.

### 4. Actions 탭 열어보기

위쪽 **Actions** 탭을 누릅니다.
"I understand my workflows, go ahead and enable them" 버튼이 보이면 눌러 주세요.
워크플로 네 개가 번호 순서로 보이면 준비 끝입니다.

```
0. 규정 수집 (매일 자동)      1. 진료비 확인 요청
2. 진료비 청구 판단           9. 요청 기록 지우기
```

---

## 실습 1 — 규정 받아 오기 (약 7분)

### 하는 일

**Actions** → **0. 규정 수집 (매일 자동)** → 오른쪽 **Run workflow** → **Run workflow**

### 무슨 일이 일어나나

파이썬이 심평원·복지부 공지사항을 받아 오고, **무엇이 우리 청구에 닿는지 고르는 판단만**
AI가 합니다. 닿는 변경이 있으면 `rules/HIRA_RULES.md` 에 **추가하는** PR을 엽니다.
기존 항목은 지우지 않습니다 — 2025-12-30 진료분은 그때의 규정으로 판단해야 하니까요.

### 기다리는 동안

저장소 첫 화면에서 **`rules/`** → **`HIRA_RULES.md`** 를 열어 보세요. 규정 5건이 **이미
머지된 상태로** 복사돼 왔습니다. 실습 2·3의 판단도 이 파일을 보고 내려집니다.

```markdown
## 치과 급여(100대100) 일부 코드 수가삭제
- 규정 ID: HIRA-2026-002
- 시행일: 2026-08-14
- 변경 유형: 코드 삭제
- 대상: AY102 · AY112
```

**공고일과 시행일이 다릅니다.** `## 걷기검사 신설` 항목도 보세요 — `F6051`·`F6052`·
`F6053` 이 **2026-07-31 신설**입니다. 그 전에는 없던 코드라는 뜻입니다.

### 결과 확인

**Pull requests** 탭에서 열린 PR을 읽습니다. 공고일·시행일·대상 코드가 제대로 적혔는지
보고, 맞으면 **Merge pull request**.

닿는 변경이 없어도 PR은 열립니다. 그때는 "무엇을 언제부터 언제까지 보았고 왜 기록하지
않았는지"만 담깁니다. 확인했다는 사실도 기록이기 때문입니다.

> **머지가 곧 승인입니다.** 사람이 머지한 규정만 다음 판단에 쓰입니다.

> 이 실습을 건너뛰어도 다음 실습은 그대로 됩니다. 규정 5건은 복사할 때부터 파일에
> 들어 있고, 수집은 거기에 **더하는** 일이기 때문입니다. 시간이 없으면 실습 2로 가세요.

---

## 실습 2 — 규정에 걸리는 환자 (약 6분)

### 하는 일

**Actions** → **1. 진료비 확인 요청** → **Run workflow**

- 환자 ID: **`P00011`**
- 진료일: 비워 둡니다
- 모델: **`auto`** (유료면 `haiku`)
- **Run workflow** 클릭

### 무슨 일이 일어나나

30초쯤 뒤 요청이 끝나고 요약에 "진료비 판단을 시작했습니다"가 뜹니다. 그 링크로
**2. 진료비 청구 판단** 실행에 들어갑니다. 3~6분 걸립니다.

```
P00011 로 조회  →  진료 3건  →  결과에서 환자 번호를 뺀다  →  AI 에게 넘긴다
```

### 결과 확인

실행 페이지 아래 **Artifacts** 의 **`billing-report`** 를 내려받아 열면, **만 82세 ·
여성 · 의료급여(2종)** 환자의 진료 3건이 이렇게 갈립니다.

| 진료일 | 항목 | 판단 |
|---|---|---|
| 2026-03-27 | CONTINUOUS & BOLUS PCA (`J4310055`) | 조건부 |
| 2026-08-12 | 코로나19 응급용 선별검사 (`D6583066`) | **급여 인정** |
| 2026-08-18 | 치과 급여(100대100) 항목 (`AY102`) | **불인정** |

마지막 줄이 이 실습의 그림입니다. `AY102` 는 **2026-08-14 자로 삭제된 코드**인데
진료일이 **2026-08-18** 입니다. 없어진 코드로 110,000원이 청구돼 있고, 그중
104,500원이 환자 본인부담으로 잡혀 있습니다.

보고서에는 판단마다 **적용 규정 · 시행일 · 진료일 비교**가 함께 적힙니다.
담당자가 확인할 수 없는 답은 쓸모가 없기 때문입니다.

---

## 실습 3 — 그대로 통과하는 환자 (약 6분)

### 하는 일

같은 자리에서 환자 ID에 **`P00001`** 을 넣고 **Run workflow**.

### 결과 확인

같은 규정, 같은 기간인데 **전부 통과합니다.**

| 진료일 | 항목 | 판단 |
|---|---|---|
| 2026-02-12 | CONTINUOUS & BOLUS PCA (`J4310055`) | **급여 인정** |
| 2026-02-12 | 전파절삭기 (`M2041029`) | **급여 인정** |
| 2026-05-19 | 코로나19 응급용 선별검사 (`D6583066`) | **급여 인정** |

세 건 다 **기록된 규정에 닿는 코드가 아닙니다.** 규정이 있다고 다 걸리는 것이 아니라,
**그 환자의 그 진료일에 닿는 규정이 있을 때만** 걸립니다.

> 시간이 남으면 **`P00013`** 도 넣어 보세요. 반대 방향으로 걸립니다 — `F6051` 걷기검사를
> **시행일(2026-07-31) 이전** 진료분에 청구했습니다.

---

## 실습 4 — 무엇이 남았는지 확인 (약 3분)

판단 결과에는 그 환자의 진료 내용이 들어 있습니다. 그래서 저장소에 남기지 않습니다.

| 볼 곳 | 있어야 하는 것 |
|---|---|
| **Issues** 탭 | 0건 |
| **Pull requests** 탭 | 실습 1의 규정 PR 말고는 없음 |
| 판단 실행의 로그 | 환자 번호도 진료내역도 없음 (검색해 보세요) |
| 판단 실행의 Artifacts | `billing-report` — 7일 뒤 사라짐 |

환자 번호가 닿는 곳은 **요청 실행의 입력값 한 곳뿐**이고, 그것도 **9. 요청 기록 지우기**
가 30분마다 돌며 지웁니다.

---

## 잘 안 될 때

| 증상 | 원인과 해결 |
|---|---|
| `One or more containers failed to start` | 준비 2번의 시크릿을 빠뜨렸습니다. 등록하고 다시 실행하세요. |
| "해당 환자 ID의 진료내역이 없습니다" | 그 번호에 진료가 없습니다. `P00011` · `P00001` · `P00013` 으로 해 보세요. |
| 판단 실행이 안 보인다 | 요청 요약의 링크를 누르세요. 시작까지 30초쯤 걸립니다. |
| 5분째 노란 점이 돌고 있다 | 정상입니다. AI가 규정을 읽고 대조하는 중입니다. |
| 규정 PR 대신 아무 일도 없다 | 준비 3번을 빠뜨렸습니다. 설정하고 다시 실행하세요. |
| 판단이 실패하고 로그에 `The requested model is not supported` | 요금제가 그 모델을 안 줍니다. 모델을 **`auto`** 로 두고 다시 실행하세요. |
| 실행은 성공인데 빨간 주석이 하나 있다 (`safe-outputs-items`) | 정상입니다. 판단 결과를 저장소에 게시하지 않아 게시 목록 파일이 만들어지지 않습니다. |

---

## 오늘 가져가실 것

- **규정은 공고된 날부터 적용되지 않습니다.** 진료일과 시행일을 대조해야 답이 나오고,
  이 대조는 사람이 매번 하기에 너무 잦습니다.
- **AI에게 환자 식별정보를 주지 않고도** 청구 판단을 시킬 수 있습니다. 주고 나서 가리는
  것이 아니라, 조회 결과에서 애초에 빼면 됩니다.
- **머지가 곧 승인입니다.** 누가 언제 어떤 규정을 반영했는지 저장소에 남습니다.
- **판단 결과는 남기지 않는 편이 낫습니다.** 그 환자의 진료 내용이니까요.

모든 판단은 청구 전 원무 담당자의 확인이 필요합니다.
